# ERRORS.md — Failure Log

<!-- SELF-MAINTENANCE — read every session, then act:
1. READ this file before starting work — check whether your planned approach already failed
   here, so you don't re-try a known dead end.
2. APPEND an entry under "## Failures" (NEWEST FIRST) the moment a real failure or gotcha is
   confirmed: a bug, a wrong assumption, a command that broke, an approach that didn't work.
3. ONE failure per entry. Always record the FIX or the rule learned — an unresolved error
   with no lesson is noise. Absolute dates (YYYY-MM-DD).
4. CURATE: keep the newest ~20 detailed. Fold resolved/stable ones into "## Known gotchas"
   as one-line rules. Delete anything no longer true. If over ~400 lines, compact now.

Entry template:
### YYYY-MM-DD — <symptom in <=10 words>
- Symptom: <what went wrong; exact error text if useful>
- Cause: <root cause, once known>
- Fix / rule: <what resolved it, or the rule to avoid it>   [status: resolved | open]
-->

## Failures
<!-- newest first -->

## Known gotchas
<!-- durable one-line rules distilled from past failures -->
